---
- name: check 
  stat: path={{ etx0 }}
  register: fm
- name: aaa
  shell: echo "echo "ffffffff" > /sys/class/net/em2/queues/tx-2/xps_cpus" >> /etc/rc.local
  when: fm.stat.exists
# - name: create
#  command: echo "echo "ffffffff" > /sys/class/net/em2/queues/tx-2/xps_cpus" >> /etc/rc.local
#  command: echo aaa > /tmp/ccc.txt
#  shell: echo "1111" > /tmp/aaaa.txt
